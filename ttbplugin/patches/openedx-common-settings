# mcdaniel: the following settings originate from https://github.com/turn-The-Bus/tutor-contrib-ttbplugin
# -----------------------------------------------------------------------------
# Google Firebase credentials
GOOGLE_FIREBASE_SERVICE_ACCOUNT_JSON={{ TTB_GOOGLE_FIREBASE_SERVICE_ACCOUNT_JSON }}
INDTTB_GOOGLE_FIREBASE_CONFIG_JSON={{ TTB_GOOGLE_FIREBASE_CONFIG_JSON }}
GOOGLE_FIREBASE_DATABASE_URL='{{ TTB_GOOGLE_FIREBASE_DATABASE_URL }}'
TOASTR_CUSTOM_CSS_URL='{{ TTB_TOASTR_CUSTOM_CSS_URL }}'

# Add the Firebase credentials to the MFE config API so that we
# can access it in the frontend.
MFE_CONFIG['GOOGLE_FIREBASE_CONFIG'] = {{ TTB_GOOGLE_FIREBASE_CONFIG_JSON }}
MFE_CONFIG['TOASTR_CUSTOM_CSS_URL'] = '{{ TTB_TOASTR_CUSTOM_CSS_URL }}'
# -----------------------------------------------------------------------------
